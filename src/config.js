// Guild Sim — core config
// Small, cross-cutting constants that don't belong to a single system. Loaded
// first so everything else can rely on them.

// Cost to hire a newbie adventurer, in gold.
const HIRE_COST = 1000;

// Roster capacity before any upgrades.
const BASE_MAX_ADVENTURERS = 3;
